create:
  database:
    file: database.yml
  auth:
    file: auth.yml
  config:
    file: config.yml
  endpoint:
    file: endpoint.yml

deploy:
  bless:
    - file
    - auth
    - config
    - endpoint